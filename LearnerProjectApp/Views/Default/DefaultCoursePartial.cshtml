@using EntityLayer.Entities
@using LearnerProjectApp.Models
@model Tuple<List<Course>, List<CourseReviewViewModel>>

<div class="untree_co-section bg-light">
    <div class="container">
        <div class="row justify-content-center mb-5">
            <div class="col-lg-7 text-center" data-aos="fade-up" data-aos-delay="0">
                <h2 class="line-bottom text-center mb-4">Son Eklenen Kurslar</h2>
                <p>Alanında uzman eğitmenler tarafından hazırlanan kurslarımızı inceleyebilirsiniz.</p>
            </div>
        </div>
        <div class="row">
            @foreach (var item in Model.Item1)
            {
                var review = Model.Item2.FirstOrDefault(r => r.CourseId == item.CourseId);
                <div class="col-12 col-sm-6 col-md-6 col-lg-4 mb-4 mt-4 mb-lg-0">
                    <div class="custom-media">
                        <a href="#"><img src="@item.ImageUrl" alt="Image" class="img-fluid"></a>
                        <div class="custom-media-body">
                            <div class="d-flex justify-content-between pb-3">
                                <div class="text-primary"><span class="uil uil-book-open"></span> <span>43 lesson</span></div>
                                <div class="review"><span class="icon-star"></span> <span>@(review != null ? review.ReviewValue.ToString("0.00") : "Değerlendirme Yok")</span></div>
                            </div>
                            <h3>@item.CourseName</h3>
                            <p class="mb-4">@(item.Description.Length > 30 ? item.Description.Substring(0, 20) : item.Description)</p>
                            <div class="border-top d-flex justify-content-between pt-3 mt-3 align-items-center">
                                <div><span class="price">@item.Price ₺</span></div>
                                <div><a href="/Default/CourseDetails/@item.CourseId">Kurs Detayı</a></div>
                            </div>
                        </div>
                    </div>
                </div>
            }
        </div>
    </div>
</div>
